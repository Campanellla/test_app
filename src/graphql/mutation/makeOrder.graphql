#import '../fragment/User/info.graphql'
#import '../fragment/TimeSlot.graphql'
#import '../fragment/Voucher/normal.graphql'

mutation MakeOrder($input: makeOrderInput!) {
  makeOrder(input: $input) {
    id
    amount
    voucher {
      ...voucher
    }
    buyer {
      ...user_info
    }
  }
}
