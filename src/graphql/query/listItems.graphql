#import "../fragment/Appartment/info.graphql"
#import "../fragment/Voucher/info.graphql"

query ListItems($sort: String) {
  listItems(sort: $sort) {
    appartment {
      ...appartment_info
    }
    voucher {
      ...voucher_info
    }
  }
}
